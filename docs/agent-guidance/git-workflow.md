# Git Workflow

- Use conventional commits for documentation changes, for example
  `docs(siren): add v2.3.0 changelog`.
- Keep home-level configuration, generated artifacts, runtime state, and
  unrelated repository changes outside documentation commits unless required by
  the documentation change.
